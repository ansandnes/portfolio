# web

Next.js 16 (App Router) + React 19 + TypeScript + Tailwind CSS v4.

Setup, scripts, environment variables, and deployment are documented in the
[repository README](../README.md). The architecture and the phased rebuild plan
are in [`../REBUILD_PLAN.md`](../REBUILD_PLAN.md).

```bash
cp .env.example .env.local   # fill in API_KEY_GEMINI
npm install
npm run dev
```
